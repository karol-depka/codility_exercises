import {solution} from "./solution";
import {fillRandomInts, getRandomIntMaxEcl} from "../../testUtils/testUtils";

describe('fut', () => {

  it(`codility example 1`, () => {
    expect(solution( [1, 4, -3])).toEqual(1);
  });

  it(`codility example 2`, () => {
    expect(solution( [-8, 4, 5, -10, 3])).toEqual(3);
  });

  it(`just 1`, () => {
    expect(solution( [9])).toEqual(18);
  });

  it(`just 1 neg`, () => {
    expect(solution( [-9])).toEqual(18);
  });

  // it(`empty`, () => {
  //   expect(solution( [])).toEqual(0);
  // });
  //
  // it(`1`, () => {
  //   expect(solution( [9])).toEqual(0);
  // });
  //
  // it(`2 intersecting`, () => {
  //   expect(solution( [3, 3])).toEqual(1);
  // });
  //
  // it(`2 just touching`, () => {
  //   expect(solution( [0, 1])).toEqual(1);
  // });
  //
  // it(`2 not intersecting`, () => {
  //   expect(solution( [0, 0])).toEqual(0);
  // });
  //
  // it(`2; 1 contains other`, () => {
  //   expect(solution( [1, 5])).toEqual(1);
  // });
  //
  // it(`2; 1 contains other, reverse`, () => {
  //   expect(solution( [5, 1])).toEqual(1);
  // });



  // it(`6 numbers`, () => {
  //   expect(solution( [-5,-6,-7, 9, 10, 11])).toEqual(-210);
  // });
  //
  // it(`7 numbers`, () => {
  //   expect(solution( [-5,-6,-7, 9, 10, 11, 13])).toEqual(-210);
  // });

  // it(`handles random data`, () => {
  //   for ( let i = 0; i< 100_000; i++ ) {
  //     const arraySize = getRandomIntMaxEcl(3, 10)
  //     const array = fillRandomInts(arraySize, -10, 11)
  //     // expect(solution( array)).toEqual(solutionBruteForce( array));
  //
  //   }
  // });


});
